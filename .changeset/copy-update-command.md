---
"@eya46/dsh-plugin-info": patch
---

When a registry plugin has an update, copy `dsh plugin --profile <current> add <pkg>@latest` in one click. The CLI profile name is read from the running profile (usually `web`).
